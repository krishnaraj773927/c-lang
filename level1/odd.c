#include<stdio.h>

int main()
{
    int num =0;
    while( num != 10)
    {
        printf("enter the number");
        scanf("%d",&num);
    }
    printf(" congrants you have guess the number ");
    return 0;
}